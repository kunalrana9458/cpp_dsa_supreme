#include<iostream>
#include<deque>
#include<queue>
using namespace std;
int main()
{
    // queue<int> q;
    // q.push(5);
    // q.push(15);
    // q.push(25);
    // q.push(35);
    // cout << q.front()  << endl;
    // q.pop();
    // cout << q.front() << endl;
    // q.pop();
    // cout << q.size() << endl;
    // ------>>>>>> Doubly ended queue
    deque<int> dq;
    
    return 0;
}